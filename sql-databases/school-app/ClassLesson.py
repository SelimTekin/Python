class ClassLesson:

    def __init__(self, classid, teacherid, lessonid):
        if id is None: # id belirtilmediyse
            self.id = 0
        else:
            self.id = id
        self.classid = classid
        self.teacherid = teacherid
        self.lessonid = lessonid